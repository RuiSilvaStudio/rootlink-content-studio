import { CallToAction } from './CallToAction'
import { Hero } from './Hero'
import { ImageWithText } from './ImageWithText'
import { TextSection } from './TextSection'

/**
 * The full library of reusable page blocks. Add new block types here so they
 * show up in every Template's block picker.
 */
export const pageBlocks = [Hero, TextSection, ImageWithText, CallToAction]
